collatz 1 = do
  result := 0
  result
end

collatz n = do
  isEven := n == n / 2 * 2
  next := if isEven then do
    step := n / 2
    step
  end else do
    step := n * 3 + 1
    step
  end
  steps := collatz(next)
  steps + 1
end

main = collatz(10)